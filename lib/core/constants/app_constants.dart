class AppConstants {
  AppConstants._();
  static const String appName        = 'ASHU';
  static const String appTagline     = 'Advanced Streaming Hub for Users';
  static const double miniPlayerHeight = 72.0;
  static const double bottomNavHeight  = 64.0;
  static const Duration fastAnim  = Duration(milliseconds: 200);
  static const Duration medAnim   = Duration(milliseconds: 350);
  static const Duration slowAnim  = Duration(milliseconds: 600);
  static const String prefRecentSearches = 'recent_searches';
}
